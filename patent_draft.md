# 专利申请书（草案）

## 说明书摘要
本发明公开了一种基于视觉通透性驱动的自支撑随机多孔结构生成与优化方法，旨在解决增材制造中随机多孔结构无法兼顾审美随机性、精确可控性与制造自支撑能力的三难困境。本发明首先构建了一套量化的视觉通透性（VP）评价指标，该指标综合考虑了孔洞路径的直线性、长度、空间位置及方向性，并以此驱动基于改进的度数受限生成树（MBDST+）的图连接优化，生成具有高通透性的骨架；随后，利用制造感知的正交各向异性高斯核进行隐式几何初始化，内在减少初始悬挑区域；最后，通过集成可微层级AM滤波器的连续密度场优化，严谨地消除残留不支撑区域。实验证明，本发明能自动产生几何多样、通透性强且100%无需额外支撑的随机多孔结构，为景观艺术、环境工程及工业轻量化设计提供了高效的数字化方案。

---

## 权利要求书
1. 一种基于视觉通透性驱动的自支撑随机多孔结构生成与优化方法，其特征在于，包括以下步骤：
    (1) **初始化与采样**：将待处理的3D模型进行体素化处理，转换为有向距离场（SDF）；在模型内部及表面随机采样若干种子点作为高斯核中心；
    (2) **通透性驱动的图连接优化**：以种子点为节点构建连接图，利用量化的视觉通透性指标（VP Metric）对图的拓扑结构进行迭代优化，生成满足审美特征的通透骨架；
    (3) **各向异性隐式建模**：基于优化后的骨架，利用制造感知的正交各向异性高斯核（Orthotropic Gaussian Kernels）建立连续的隐式几何场，通过拉伸核函数轴向减少初始悬挑；
    (4) **层级滤波密度优化**：通过集成了可微分层级AM滤波器的密度场优化算法，对初始生成的几何进行精细修正，消除不满足悬挑角度限制的区域，得到最终的自支撑随机多孔结构。

2. 根据权利要求1所述的方法，其特征在于，所述步骤(2)中的视觉通透性指标 $VP_{path}(\mathcal{P})$ 由以下四个分量加权计算得到：
    $$VP_{path}(\mathcal{P}) = w_{ang} \cdot Ang(\mathcal{P}) + w_{len} \cdot Len(\mathcal{P}) + w_{loc} \cdot Loc(\mathcal{P}) + w_{dir} \cdot Dir(\mathcal{P})$$
    其中，$Ang(\mathcal{P})$ 表示路径直线性，$Len(\mathcal{P})$ 表示穿透深度，$Loc(\mathcal{P})$ 鼓励内部路径，$Dir(\mathcal{P})$ 衡量水平可视倾向。

3. 根据权利要求1所述的方法，其特征在于，所述步骤(2)中的图连接优化采用改进的度数受限生成树算法（MBDST+），该算法包括构建初始带权值的MBDST书，以及后续的加边（Edge Addition）和换边（Edge Replacement）操作，以在指定的边预算内最大化全局通透性分数。

4. 根据权利要求1所述的方法，其特征在于，所述步骤(3)中的正交各向异性高斯核被配置为沿打印轴向（Z轴）进行比例拉伸，其拉伸比例 $\rho = \sigma_{||} / \sigma_{\perp}$ 采样范围为 [1.0, 1.5]，且主轴方向受限于以Z轴为中心、偏转角小于15度的圆锥区域内。

5. 根据权利要求1所述的方法，其特征在于，所述步骤(4)中的可微分层级AM滤波器采用递归定义的层级沉积模拟，第k层的可打印密度 $\rho^P_k$ 定义为：
    $$\rho^P_k = S(\bar{\rho}_k, \mathcal{D}(\rho^P_{k-1}))$$
    其中，$\mathcal{D}(\cdot)$ 为形态学膨胀操作，$S(\cdot)$ 为解析可微的 SoftMin 算子，用于计算当前层密度与下方支撑域密度的最小重叠。

---

## 说明书

### 技术领域
本发明涉及增材制造（3D打印）及计算几何技术领域，具体涉及一种面向随机多孔结构的生成与拓扑优化方法。

### 背景技术
多孔结构在轻量化、热交换及景观艺术设计中具有重要价值。然而，现有的随机多孔结构生成方法（如Voronoi镶嵌或高斯随机场）在面向3D打印时面临巨大的挑战。一方面，缺乏量化的审美评价指标，难以自动化生成视觉通透感强的结构；另一方面，由于其随机性，内部往往存在大量不满足悬挑要求的区域，需要添加大量复杂的外部支撑，这严重破坏了结构的审美完整性并增加了后处理难度。

### 发明内容
本发明旨在解决上述“随机性-可控性-可打印性”之间的三难困境。本发明提出了一种解耦的优化流水线：
1. **拓扑层优化**：通过量化视觉通透性并驱动MBDST+图连接，确定最优的孔洞网络拓扑。
2. **几何层初始化**：通过正交各向异性高斯核，从源头上减少制造违规。
3. **制造层优化**：通过GPU加速的密度场拓扑优化，消除最后的悬挑违例。

### 附图说明
*   图1：本发明实施例定义的自支撑多孔结构生成流水线示意图。
*   图2：视觉通透性指标各分量计算原理图。
*   图3：各向异性高斯核对初始悬挑区域的影响对比图。

### 具体实施方式
（在此处详细展开权利要求书中的数学公式及算法流程，参考论文 Sec 3.1 - 3.5 的详细描述内容。）
例如，步骤(4)的密度场优化通过求解以下约束问题实现：
$$\min_{\mathbf{x}} \mathcal{J}(\rho^P, \rho^*) = \lambda_1 \mathcal{L}_{Dice} + \lambda_2 \mathcal{L}_{MSE}$$
$$\text{s.t.} \quad G_{ov}(\mathbf{x}) = ||\bar{\rho} - \rho^P||_1 \leq \epsilon_{tol}$$
该问题采用多维度移动渐近线法（MMA）在GPU上并行求解。
